import React from 'react';
import BlogList from '../components/BlogList';

const Home = () => {
  return (
    <div className="container">
      <BlogList />
    </div>
  );
};

export default Home;
